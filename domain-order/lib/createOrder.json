{
  "title": "createOrder",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "command": {
      "type": "string"
    },
    "aggregate": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": ["name"]
    },
    "payload": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string", "format": "date" 
        },
        "placedby": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "quantity": {
          "type": "number"
        },
        "total": {
          "type": "number"
        },
        "shipto": {
          "type": "string"
        },
        "selected": {
          "type": "boolean"
        }
      },
      "required": ["date", "placedby", "status", "quantity", "total", "shipto", "selected"]
    }
  },
  "required": ["id", "command", "payload"]
}